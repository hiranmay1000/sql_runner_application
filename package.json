{
  "name": "sql_runner_application_root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && (python3 app.py || python app.py || py app.py)\" \"cd frontend && npm start\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
